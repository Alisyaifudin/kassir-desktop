import { add } from "./add";
import { delById } from "./del-by-id";
import { getAll } from "./get-all";
import { update } from "./update";

export const method = {
  getAll,
  delById,
  add,
  update,
};
