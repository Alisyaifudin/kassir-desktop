import { add } from "./add";
import { delById } from "./del-by-name";
import { getAll } from "./get-all";
import { update } from "./update";

export const social = {
  getAll,
  update,
  delById,
  add,
};
