import { Effect } from "effect";
import { JSX } from "react";

// export function Unwrap<T extends () => JSX.Element, E, R>({ component }: { component: Effect.Effect<T, E, R> }) {
//   return Effect.gen(function*(){

//   })
// }

